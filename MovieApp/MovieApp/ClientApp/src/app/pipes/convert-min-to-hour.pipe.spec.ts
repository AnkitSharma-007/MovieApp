import { ConvertMinToHourPipe } from './convert-min-to-hour.pipe';

describe('ConvertMinToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMinToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
